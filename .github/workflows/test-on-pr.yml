name: Test and Lint on PR

on:
  pull_request:
    branches: [ master, main ]

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'
      
      - name: Display Go version
        run: go version
      
      - name: Install dependencies
        run: go mod download
      
      - name: Run tests
        id: test
        run: |
          go test -v ./...
          echo "exit_code=$?" >> $GITHUB_OUTPUT
      
      - name: Report test results
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const testExitCode = process.env.TEST_EXIT_CODE === '0' ? true : false;
            
            if (!testExitCode) {
              const { owner, repo, number } = context.issue;
              
              github.rest.issues.createComment({
                owner,
                repo,
                issue_number: number,
                body: `❌ Tests failed. Please check the logs for details.`
              });
              
              core.setFailed('Tests failed');
            }
        env:
          TEST_EXIT_CODE: ${{ steps.test.outputs.exit_code || '0' }}

  frontend:
    name: Verify Web Frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type-check frontend
        run: npm run typecheck:web

      - name: Build frontend
        run: npm run build:web

      - name: Verify committed frontend assets
        shell: bash
        run: |
          if ! git diff --quiet -- server/web/dist || \
            [[ -n "$(git ls-files --others --exclude-standard -- server/web/dist)" ]]; then
            git status --short -- server/web/dist
            git diff -- server/web/dist
            exit 1
          fi

  lint:
    name: Run Linting
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'
      
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          args: --timeout=5m
      
  build:
    name: Verify Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'
      
      - name: Build
        run: go build -v ./...
        
  pr-status:
    name: PR Status
    needs: [test, frontend, lint, build]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check test and lint status
        uses: actions/github-script@v7
        with:
          script: |
            const testStatus = '${{ needs.test.result }}' === 'success';
            const frontendStatus = '${{ needs.frontend.result }}' === 'success';
            const lintStatus = '${{ needs.lint.result }}' === 'success';
            const buildStatus = '${{ needs.build.result }}' === 'success';
            
            if (testStatus && frontendStatus && lintStatus && buildStatus) {
              const { owner, repo, number } = context.issue;
              
              github.rest.issues.createComment({
                owner,
                repo,
                issue_number: number,
                body: `✅ All checks passed successfully! Ready to merge.`
              });
            } else {
              core.setFailed('One or more checks failed');
            }
